{
  "name": "candidate-technical-assessment-system",
  "version": "1.0.0",
  "description": "Dynamic Technical Assessment Platform for candidate shortlisting",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "setup": "cd backend && npm install && node setup.js",
    "seed": "cd backend && node seed.js",
    "setup:all": "cd backend && npm install && node setup.js && node seed.js",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": ["assessment", "quiz", "technical-test"],
  "license": "MIT"
}
